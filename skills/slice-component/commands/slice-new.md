---
description: Port a page/component from the prototype repo into production for the first time (page doesn't exist in production yet)
argument-hint: [menu name, route, or literal path in the prototype repo]
---

Follow the **`slice-new`** procedure in this skill's `SKILL.md`, using `$ARGUMENTS` as the target. Confirm the resolved prototype path and production destination with the user before copying anything if resolution was ambiguous.
